### General

# Set escape delay to 0
set -sg escape-time 0

# Allow mouse usage
set -g mouse on

# Set prefix to Alt + b
unbind C-b
set-option -g prefix M-b
bind-key M-b send-prefix

# Start windows at 1 (not 0)
# Automatically renumber windows when one is closed
set -g base-index 1
set -g renumber-windows on

# Use vim bindings
set-window-option -g mode-keys vi


### Status bar

# Set status bar on top
set-option -g status-position top

# Enlarge right status bar
set -g status-right-length 65

# Right status bar
set-option -g status-right "🕖  %H:%M:%S | 📅  (%a) %d.%m.%y "


### Open/close window/pane

# New window: Alt + T
bind-key -n C-t new-window

# Split window vertically: Alt + "\"
bind-key -n M-\\ split-window -h

# Split window horizontally: Alt + |
bind-key -n M-\| split-window -v

# Close pane: Alt + W
bind-key -n C-w kill-pane

# Close window: Alt + Q
# bind-key -n C-q kill-window


### Switch pane/window

# Switch window: Alt + number (0-9)
bind-key -n M-0 select-window -t :0
bind-key -n M-1 select-window -t :1
bind-key -n M-2 select-window -t :2
bind-key -n M-3 select-window -t :3
bind-key -n M-4 select-window -t :4
bind-key -n M-5 select-window -t :5
bind-key -n M-6 select-window -t :6
bind-key -n M-7 select-window -t :7
bind-key -n M-8 select-window -t :8
bind-key -n M-9 select-window -t :9

# Switch pane: Alt + H, J, K, L
bind-key -n M-h select-pane -L
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-l select-pane -R

# Enlarge pane: Alt + Shift + H, J, K, L
bind-key -n M-H resize-pane -L 5
bind-key -n M-J resize-pane -D 5
bind-key -n M-K resize-pane -U 5
bind-key -n M-L resize-pane -R 5

